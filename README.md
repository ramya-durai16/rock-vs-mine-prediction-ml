# rock-vs-mine-prediction-ml
This project predicts whether a sonar signal corresponds to a Rock (R) or a Mine (M) using Logistic Regression. The dataset contains 60 sonar frequency features, and the model is trained to classify underwater objects with good accuracy.
